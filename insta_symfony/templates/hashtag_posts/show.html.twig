{% extends 'base.html.twig' %}

{% block title %}HashtagPosts{% endblock %}

{% block body %}
    <h1>HashtagPosts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hashtag_post.id }}</td>
            </tr>
            <tr>
                <th>Id_hashtag</th>
                <td>{{ hashtag_post.idHashtag }}</td>
            </tr>
            <tr>
                <th>Id_posts</th>
                <td>{{ hashtag_post.idPosts }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hashtag_posts_index') }}">back to list</a>

    <a href="{{ path('hashtag_posts_edit', {'id': hashtag_post.id}) }}">edit</a>

    {{ include('hashtag_posts/_delete_form.html.twig') }}
{% endblock %}
