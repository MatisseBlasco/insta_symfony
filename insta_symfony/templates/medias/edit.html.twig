<!DOCTYPE html>

<title>Edit Medias</title>

{% block body %}
    <h1>Edit Medias</h1>

    {{ include('medias/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('medias_index') }}">back to list</a>

    {{ include('medias/_delete_form.html.twig') }}
{% endblock %}
