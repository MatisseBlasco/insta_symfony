{% extends 'base.html.twig' %}

{% block title %}Like{% endblock %}

{% block body %}
    <h1>Like</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ like.id }}</td>
            </tr>
            <tr>
                <th>Id_posts</th>
                <td>{{ like.idPosts }}</td>
            </tr>
            <tr>
                <th>Id_users</th>
                <td>{{ like.idUsers }}</td>
            </tr>
            <tr>
                <th>Id_comments</th>
                <td>{{ like.idComments }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('like_index') }}">back to list</a>

    <a href="{{ path('like_edit', {'id': like.id}) }}">edit</a>

    {{ include('like/_delete_form.html.twig') }}
{% endblock %}
